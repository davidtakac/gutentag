name: gutentag
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http:
  intl:
  json_annotation:
  webview_flutter:
  injectable:
  get_it:
  equatable:
  bloc:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  build_runner:
  json_serializable:
  injectable_generator:

flutter:
  uses-material-design: true
  generate: true
